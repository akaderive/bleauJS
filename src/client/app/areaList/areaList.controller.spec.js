/* jshint -W117, -W030 */
describe('AreaListController', function() {
    var controller;

    /*beforeEach(function() {
        bard.appModule('app.areaList');
        //bard.inject('$q', '$controller', '$log', '$rootScope', '$sessionStorage');
        bard.inject('$q', 'logger', '$sessionStorage', '$scope', 'dataservice');
    });

    beforeEach(function () {
        var scope = $rootScope.$new();
        controller = $controller('AreaListController', {$scope:scope});
        $rootScope.$apply();
    });

    bard.verifyNoOutstandingHttpRequests();

    describe('AreaList controller', function() {
        it('should be created successfully', function () {
            expect(controller).to.be.defined;
        });

        describe('after activate', function() {
            it('should have logged "Activated"', function() {
                expect($log.info.logs).to.match(/Activated/);
            });
        });
    });*/
});
