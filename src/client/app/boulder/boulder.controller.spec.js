/* jshint -W117, -W030 */
describe('BoulderController', function() {
    var controller;
    /*
    beforeEach(function() {
        bard.appModule('app.boulder');
        bard.inject('$controller', '$log', '$rootScope');
    });

    beforeEach(function () {
        controller = $controller('BoulderController');
        $rootScope.$apply();
    });

    bard.verifyNoOutstandingHttpRequests();

    describe('Boulder controller', function() {
        it('should be created successfully', function () {
            expect(controller).to.be.defined;
        });

        describe('after activate', function() {
            it('should have title of Boulder', function() {
                expect(controller.title).to.equal('Boulder');
            });

            it('should have logged "Activated"', function() {
                expect($log.info.logs).to.match(/Activated/);
            });

            it('should have a static populated listOfBoulders', function() {
                expect(controller.listOfBoulders.length).to.equal(6);
            });
        });
    });*/
});
